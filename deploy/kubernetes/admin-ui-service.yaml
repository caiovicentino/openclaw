apiVersion: v1
kind: Service
metadata:
  name: admin-ui-service
  namespace: openclaw
  labels:
    app.kubernetes.io/name: admin-ui
    app.kubernetes.io/part-of: openclaw
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: admin-ui
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
