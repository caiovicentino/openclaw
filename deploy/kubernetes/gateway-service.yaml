apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: openclaw
  labels:
    app.kubernetes.io/name: gateway
    app.kubernetes.io/part-of: openclaw
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gateway
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
