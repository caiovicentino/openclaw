apiVersion: v1
kind: Service
metadata:
  name: admin-api-service
  namespace: openclaw
  labels:
    app.kubernetes.io/name: admin-api
    app.kubernetes.io/part-of: openclaw
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: admin-api
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
